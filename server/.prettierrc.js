module.exports = {
  bracketSpacing: true, // Print spaces between brackets in object literals
  jsxBracketSameLine: false, // Put > of multi-line JSX elements at the end of the last line
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'es5', // Trailing commas where valid in ES5 (objects, arrays, etc.)
  semi: true, // Print semicolons at the ends of statements
  tabWidth: 2, // Specify the number of spaces per indentation-level
  useTabs: false, // Indent lines with tabs instead of spaces
  printWidth: 100, // Specify the line length that the printer will wrap on
  arrowParens: 'always', // Include parentheses around a sole arrow function parameter (e.g. (x) => x)
  endOfLine: 'lf', // Line Feed only (\n), common on Linux and macOS as well as inside git repos
};
